# Package
version       = "0.1.0"
author        = "Birb Party"
description   = "Minimal idle clicker built with naylib (raylib)."
license       = "MIT"
srcDir        = "src"
bin           = @["idle_clicker"]

# Dependencies
requires "nim >= 2.0.0"
requires "naylib >= 5.0.0"
